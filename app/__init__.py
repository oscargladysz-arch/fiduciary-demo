"""Tark workspace API (decision 8.8): FastAPI on Render's free tier, no
secrets in this package, Supabase Auth for identity and row-level security
for tenancy (rule 22). Run: uvicorn app.main:app --port 8000"""
