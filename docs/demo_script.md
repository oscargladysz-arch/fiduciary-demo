# Tark Demo Script — 7 minutes (census build)
### Setup: reset per docs/INVESTOR_DEMO.md; land on the Universe view. Fallback: docs/screenshots/.

**0:00 — The Funnel.** "Start with the honest picture of the market. 72,502 private pooled funds raised money in the last 24 months — the dark universe: no NAV, no fee table, nothing a fiduciary can inspect. Below it, the entire REGISTERED alternatives universe: 3,599 wrappers, enumerated from SEC filing behavior — 245 interval funds, 516 tender CEFs, 378 BDCs, over a thousand non-traded REITs. This is the whole addressable universe, counted, and we can show you every one."

**1:00 — Universe screener.** "Every row is T1 — structured filing data: machine-read from N-CEN, XBRL, and the EDGAR submissions stream. No AI judgment anywhere on this page. Wrapper classes come from what a fund FILED, never from its name — the name gives you at most a badged 'hint' that is excluded from filters by default. Filter: interval funds, structured assets over $1B. Every number carries its dataset, its accession, its as-of date — hover any cell."

**1:45 — Entity detail, the honest gap.** Click a large unevaluated fund: *"Here's what T1 knows: auditor, interval self-classification cross-checked against actual N-23C3A filings, average net assets, latest annual. And here's what it says when it doesn't know: 'NAV per share — unavailable-structured, issuer-custom XBRL tagging.' The census refuses to guess. The 'Evaluate this fund' panel is the promotion pipeline — one command scaffolds the 54-cell record with the census answers pre-filled and marked STRUCTURED, a distinct tier from anything a model wrote."*

**2:45 — The promotion, live proof.** "We ran that pipeline twice this build. Blue Owl Credit Income — a non-traded BDC with $19.8B of net assets that the census flagged with 46 quarterly tenders — and CION Ares Diversified Credit, an interval fund with a dual-adviser structure. Both went census → R1 identity check (we caught Blue Owl's Owl Rock rename in the SEC record) → full six-factor extraction, every cell cited, cohort placement argued in the decision log. The roster is now sixteen products, and each one links back to its census row."

**3:45 — Screener, 16 rows.** "The evaluated layer: sixteen products, three depth-and-provenance tiers, every cell labeled. Private credit cohort: five rows now — two wrappers, deliberately cross-wrapper, the fee-base traps caught by the same detector. The structured cells from the census show their own blue badge — 'structured filing data' — because the fiduciary deserves to know which facts came from a machine-readable dataset and which came from a model reading a prospectus."

**4:45 — The tier walk.** Coverage view: *"T1: 3,599 funds censused. T2: sixteen evaluated, every cell extracted-and-cited, zero unresolved. T3: the cells a human has re-checked and signed — and ONLY those say 'verified.' The verification queue ranks structured cells LAST: a machine transcription of a structured dataset is the least likely thing in this record to be wrong. That ordering is itself a statement about honesty."*

**5:45 — Compare + benchmark, unchanged discipline.** Cmd+K → "compare cclfx cion_ares": *"caveat banner assembles itself from the wrapper matrix. The benchmark engine still refuses composites it can't defend, still escalates listed-fund premium gaps, still shows its rubric beside your override. Nothing in the census build weakened a single gate — the gates only grew."*

**6:30 — Close.** "The pitch used to be: fourteen products evaluated the honest way. Now it's: the ENTIRE registered alts universe, searchable at structured-data depth; sixteen products at evidence depth; a human-verified core; and a one-command pipeline that turns any of the 3,599 into the next evaluated product — with the machine's tier and the human's tier never confused. That's the Bloomberg-for-alts gap, closed the honest way."
